'use strict';

const styles = {
    dot: size => {
        return {
            height: `${size}vh`,
            width: `${size}vh`,
            marginLeft: "10px",
            backgroundColor: "black",
            borderRadius: "50%",
            display: "inline-block"
        }
    },
};

export default styles;