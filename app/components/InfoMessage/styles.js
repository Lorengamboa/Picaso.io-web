'use strict';

const infoMessageStyles = {
    container: color => {
        return {
            color,
            display: "flex",
            alignItems: "center",
            marginBottom: "10px"
        }
    }
};

export default infoMessageStyles;