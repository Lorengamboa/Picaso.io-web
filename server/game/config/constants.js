'use strict';

const DRAW_COLORS = ['RED', 'BLUE', 'GREEN', 'YELLOW', 'BLACK'];

const GAME_STATE = {
  WAITING: 'waitting',
  STARTING: 'starting',
  PLAYING: 'playing',
  VOTING: 'voting'
};

module.exports = { DRAW_COLORS, GAME_STATE };
